# CLAUDE.md

## 프로젝트 개요

양자택일 결정 도우미 웹 서비스.

사용자가 **두 개의 선택지**와 **평가 기준별 가중치·점수**를 입력하면:

1. 가중합(weighted sum)으로 각 선택지의 총점을 계산해 추천안을 제시한다.
2. "어느 기준이 결과를 갈랐는지"를 **기준별 기여도**로 분해해 근거를 보여준다.

핵심 구조: **입력 폼 → 결과(추천안) + 근거(기여도 분석)**

## 기술 스택

- **Next.js** (App Router) + **TypeScript**
- **Tailwind CSS**
- 상태 관리: React 기본 훅(useState 등)으로 충분. 별도 라이브러리 없음.
- 차트: 기여도 막대그래프는 우선 CSS/div 기반으로 구현(외부 차트 라이브러리 없이). 필요 시 나중에 검토.

## 범위 (현재 단계)

### 포함
- 페이지 **1개** (`app/page.tsx`): 입력 폼과 결과·근거 출력이 한 화면에 있음.
- 계산 로직은 순수 함수로 분리 (`lib/` 하위).
- 평가 기준 목록의 기본값은 **로컬 JSON 파일**에서 읽는다 (`data/criteria.json` 등).

### 제외 (지금 하지 않음)
- 로그인 / 인증
- 결제
- 실시간 기능 (WebSocket, SSE 등)
- 외부 API 호출 및 API 키
- **DB 연동 (Supabase 등) — 나중에 붙임**
- **Vercel 등 배포 설정 — 나중에**

## 데이터

- 현재는 **로컬 JSON 파일**만 사용 (`data/` 디렉터리).
- 예: 기본 평가 기준 세트, 예시 시나리오 등.
- DB는 이후 단계에서 도입 예정이므로, 데이터 접근부는 JSON을 읽는 얇은 계층으로 분리해 교체가 쉽도록 한다.

## 화면 구성 (page.tsx)

1. **선택지 입력**: 옵션 A / 옵션 B 이름.
2. **기준 입력**: 기준명 + 가중치(예: 1~5) + 옵션별 점수(예: 1~10). 기준 행 추가/삭제 가능. 기본 기준은 JSON에서 로드.
3. **결과**: 총점 비교, 추천안 표시.
4. **근거**: 기준별 기여도(가중치 × 점수차) 막대그래프, 결과를 뒤집는 데 필요한 임계 변화 등 설명.

## 디렉터리 구조 (예정)

```
app/
  page.tsx          # 유일한 페이지: 폼 + 결과 + 근거
  layout.tsx
lib/
  score.ts          # 가중합 계산, 기여도 분해 (순수 함수)
data/
  criteria.json     # 기본 평가 기준 세트
```

## 작업 규칙

- 새 페이지를 추가하지 말 것 (단일 페이지 유지).
- 계산 로직은 `lib/`의 순수 함수로 두고 UI와 분리.
- 외부 네트워크 호출 코드를 넣지 말 것.
- 배포/DB/인증 관련 설정 파일을 생성하지 말 것 (별도 요청 시에만).
